---
title: Логика и автоматизация
lastChanged: 07.09.2026
translatedFrom: de
translatedWarning: Если вы хотите отредактировать этот документ, удалите поле «translationFrom», в противном случае этот документ будет снова автоматически переведен
editLink: https://github.com/ioBroker/ioBroker.docs/edit/master/docs/ru/logic/README.md
hash: PEbkWuzMbIUadnOX2EnHr0nuYfAYqtLH7G+66GpwPrA=
---
# Логика и автоматизация
ioBroker собирает данные: температуру, состояние переключения, время.

Именно логика превращает это в умный дом. Он отслеживает состояния, принимает решения и устанавливает другие состояния - «если балконная дверь открыта более пяти минут и отопление включено, то выключить отопление и отправить сообщение на телефон».

Существует несколько способов записать эту логику. Они не являются взаимоисключающими: в зрелой системе графические правила и JavaScript существуют бок о бок, каждый в той позиции, которая требует наименьших усилий.

## Где работает логика
Большинство методов используют **один** адаптер, [JavaScript-адаптер](/adapters/javascript).

После установки он добавляет вкладку *Скрипты* в административный интерфейс и запускает там Blockly, правила, JavaScript и TypeScript. Любому, кто хочет использовать только один из этих четырех, достаточно установить этот единственный адаптер.

Два других варианта - это пользовательские адаптеры: [узел-красный](/adapters/node-red) включает редактор потоков Node-RED, а [сцены](/adapters/scenes) сохраняет сцены без необходимости программирования.

## Маршруты вкратце
| Способ | Что это | Адаптер |
|---|---|---|
| [Блокли](/docs/logic/blockly.md) | Графические строительные блоки, которые подходят друг к другу | javascript |
| [JavaScript](/docs/logic/javascript.md) | Полный язык программирования с API скриптов ioBroker | javascript |
| [Машинопись](/docs/logic/typescript.md) | JavaScript с проверкой типов перед запуском | javascript |
| [Node-RED](/docs/logic/nodered.md) | Специализированный редактор, в котором узлы соединены линиями | node-red |
| [Node-RED](/docs/logic/nodered.md) | Специализированный редактор, в котором узлы соединены линиями | node-red |
| Сцены | Список состояний и их целевых значений, не программа | сцены |

## В какую сторону и для чего
**Сцены** - это не программа, а сохранённая ситуация: «Телевизор» устанавливает пять ламп на определённые значения. Тем, кому нужны только такие ситуации, логика не нужна, достаточно адаптера сцен. Сцены можно впоследствии вызвать любым другим способом.

Правила - это самый быстрый способ начать работу, если автоматизация действительно следует шаблону *если это состояние, то то действие*. Ничего не собирается, а лишь выбирается.

Blockly - это правильный выбор, когда в дело вступают множественные условия, задержки или циклы, и никто не хочет писать код. Это не игрушечная версия: базовые компоненты охватывают большую часть API для работы со скриптами, а сгенерированный код JavaScript можно просмотреть из любого скрипта Blockly.

**JavaScript** становится полезным, когда скрипт становится сложным - множество похожих случаев, пользовательские функции, структуры данных, модули npm. Скрипт Blockly, содержащий тридцать блоков, обычно состоит из десяти строк кода JavaScript.

**TypeScript** - это JavaScript с предварительной проверкой. Он полезен для длинных скриптов, к которым вы редко обращаетесь и поэтому не помните их.

Node-RED отлично подходит для случаев, когда данные из множества источников сходятся и обрабатываются - протоколы, HTTP-запросы, очереди. Однако для сценария «если движение, то свет» это более сложный подход, поскольку он работает как отдельный процесс параллельно с ioBroker.

Один подход не исключает другой. Обычно простые автоматизации выполняются в виде правил или в Blockly, а JavaScript используется только для тех случаев, когда это было бы нецелесообразно в Blockly.

## Что применимо ко всем путям
Какой бы путь ни был выбран, три вещи всегда остаются неизменными.

**Логика реагирует, а не спрашивает.** Скрипт, который проверяет изменения каждую секунду, почти всегда ошибается. ioBroker автоматически сообщает о каждом изменении; затем логика использует этот отчет. Это не только эффективнее, но и точнее.

**Флаг ack разделяет команды и обратную связь.** Каждое состояние, помимо своего значения, содержит флаг `ack`. `ack: false` означает «это команда для устройства», `ack: true` означает «устройство сообщает об этом». Если триггер не различает эти два состояния, возникнут циклы: скрипт переключается, устройство подтверждает получение, и подтверждение снова запускает скрипт. Подробности см. в [Условия](/docs/basics/states.md).

**Состояния - это общая память.** Скрипты не используют общие переменные - даже два скрипта в одном экземпляре. Когда одному скрипту необходимо взаимодействовать с другим, он делает это через состояние.

## Начинать
1. В панели администратора, в разделе *Адаптеры*, установите адаптер JavaScript и...

Создать экземпляр.

2. Введите географические координаты в настройках экземпляра. Без них,

Восход и закат непредсказуемы, и именно это вам нужно как можно скорее.

3. Откроется новая вкладка *Скрипты*. Там нажмите на значок листа слева.

Создайте новый скрипт и выберите тип - правила, Blockly, JavaScript или TypeScript.

4. Скрипт будет выполнен только при активации с помощью кнопки воспроизведения.

Для первоначального тестирования рекомендуется запустить второй экземпляр JavaScript. В случае критической ошибки будет завершен только этот тестовый экземпляр, а не тот, который запускает систему управления отоплением. Более подробную информацию можно найти в разделе [Поиск неисправностей](/docs/logic/help.md).

## Дальше
* [Blockly](/docs/logic/blockly.md)
* [JavaScript](/docs/logic/javascript.md)
* [TypeScript](/docs/logic/typescript.md)
* [Node-RED](/docs/logic/nodered.md)
* [Отладка](/docs/logic/help.md)
* [Рекомендации](/docs/logic/examples.md)