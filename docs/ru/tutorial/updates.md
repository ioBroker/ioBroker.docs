---
title: Установите обновления
lastChanged: 08.09.2026
translatedFrom: de
translatedWarning: Если вы хотите отредактировать этот документ, удалите поле «translationFrom», в противном случае этот документ будет снова автоматически переведен
editLink: https://github.com/ioBroker/ioBroker.docs/edit/master/docs/ru/tutorial/updates.md
hash: lDDf2uNr1dAhSZ1iFc9+K7YJY8cAdMXQL4WUTZJodsY=
---
# Установите обновления

Обновления поддерживают работоспособность системы, но они также являются наиболее распространенной причиной ее внезапного выхода из строя. Обе эти проблемы можно решить, если следовать определенной последовательности и не пытаться сделать все сразу.

## Золотое правило

!> Перед каждым обновлением
[Резервная копия](/docs/config/backup.md)Не "с прошлой недели", а с сегодняшнего дня. На приготовление уходит минута, но благотворительное мероприятие легко может длиться весь вечер.

Вот ещё два правила, доказавшие свою эффективность на практике:

- **Не всё сразу.** Если после пяти обновлений что-то пойдёт не так, вы не узнаете, какое именно.
- **Не непосредственно перед отъездом.** Неудачное обновление требует времени.

## Хронологический порядок

1. **Node.js**если ожидается выход новой версии.
2. **js-контроллер**.
3. **адаптер**.

Обновление снизу вверх часто работает хорошо, но иногда может привести к ошибкам: для нового адаптера может потребоваться более новая версия js-контроллера, а это, в свою очередь, может потребовать более новой версии Node.js.

## Обновить адаптер

В всаднике [адаптер](/docs/admin/adapter.md)
Адаптеры с более новой версией выделены. Перед нажатием стоит проверить список изменений адаптера: в нем будет указано, является ли изменение исправлением ошибки или модификацией, требующей дальнейшей доработки.

После обновления: проверьте, запущен ли экземпляр, а затем...
[протокол](/docs/admin/log.md) видеть.

Оставайтесь в репозитории **стабильный**Представленные там версии были протестированы.
_бета_ Это предназначено для помощи в тестировании, а не для системы, которая должна работать. См.
[Репозитории](/docs/basics/repositories.md).

## Обновите js-контроллер

Он будет **нет** Обновление во вкладке «Адаптер», но во вкладке
[Хозяева](/docs/admin/hosts.md)Если будет доступна более новая версия, там появится уведомление. В командной строке:

```bash
iobroker upgrade self
```

Во время обновления пользовательский интерфейс будет временно недоступен. Это нормально.

## Обновите Node.js

Это шаг с наиболее значительными последствиями и единственный, который выполняется вне ioBroker. Для этого ioBroker предоставляет собственную команду:

```bash
iobroker nodejs-update
```

!> Только **прямой** Используйте номера версий, т.е. версии LTS. Версии с нечетными номерами не поддерживаются. Подробности см. ниже.
[Обновите Node.js](/docs/install/updatenode.md).

## операционная система

В Linux базовая система также нуждается в обслуживании:

```bash
sudo apt update
sudo apt upgrade
```

Система, которая годами не получала обновлений безопасности, представляет больший риск, чем адаптер, отстающий на одну версию.

## Если обновление пойдёт не так

1. В [протокол](/docs/admin/log.md)
   Вы можете это увидеть. Обычно там четко указано, чего не хватает.
2. Остановите затронутый экземпляр и перезапустите его отдельно.
3. Если это не поможет, восстановите резервную копию. Инструкции по этому поводу приведены ниже.
   [Восстановить конфигурацию](/docs/trouble/restore.md)
   и подробно в
   [Восстановить](/docs/tutorial/restore.md).

А затем в [форум](/docs/trouble/forum.md)
Поспрашивайте у других. Если обновление не удается нескольким людям, опыт показывает, что вы не одиноки.