---
chapters: {"pages":{"en/adapterref/iobroker.hoymiles/README.md":{"title":{"en":"ioBroker.hoymiles"},"content":"en/adapterref/iobroker.hoymiles/README.md"},"en/adapterref/iobroker.hoymiles/docs/en/README.md":{"title":{"en":"ioBroker.hoymiles — Hoymiles HMS-xxxW-xT / HMS-xxx-xWB"},"content":"en/adapterref/iobroker.hoymiles/docs/en/README.md"}}}
translatedFrom: en
translatedWarning: Если вы хотите отредактировать этот документ, удалите поле «translationFrom», в противном случае этот документ будет снова автоматически переведен
editLink: https://github.com/ioBroker/ioBroker.docs/edit/master/docs/ru/adapterref/iobroker.hoymiles/docs/en/README.md
title: ioBroker.hoymiles - Hoymiles HMS-xxxW-xT / HMS-xxx-xWB
hash: ExFxJWfhkQQ3u7r5iGjDwRaSNNYY5EzmksmQwBlzP7A=
---
![Логотип](../../../../../en/adapterref/iobroker.hoymiles/admin/hoymiles.png)

# ioBroker.hoymiles — Hoymiles HMS-xxxW-xT / HMS-xxx-xWB

## Поддерживаемые инверторы

Этот адаптер предназначен для **микроинверторов Hoymiles HMS со встроенным модулем DTU (DTUBI) Wi-Fi (или Wi-Fi + Bluetooth)** .

**Локальный доступ** = прямое TCP/Protobuf-соединение на порту 10081. **Облачный доступ** = API S-Miles Cloud — автоматическое обнаружение, данные в реальном времени (быстрый пакетный канал \~1,5–3 с), агрегированные данные об энергопотреблении, профиль сети, включение/выключение инвертора + перезагрузка, перезагрузка DTU.

| Модель        | Строки | Локальный (TCP) | Облако | Статус                                                                                                                                |
| ------------- | :----: | :-------------: | :----: | ------------------------------------------------------------------------------------------------------------------------------------- |
| HMS-300W-1T   |    1   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-350W-1T   |    1   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-400W-1T   |    1   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-450W-1T   |    1   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-500W-1T   |    1   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-600W-2T   |    2   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-700W-2T   |    2   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-800W-2T   |    2   |        ✅        |    ✅   | **Протестировано** (локально + в облаке)                                                                                              |
| HMS-900W-2T   |    2   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-1000W-2T  |    2   |        ✅        |    ✅   | **Протестировано** (локально)                                                                                                         |
| HMS-1600DW-4T |    4   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-1800DW-4T |    4   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-2000DW-4T |    4   |        ✅        |    ✅   | Непроверенный                                                                                                                         |
| HMS-600-2WB   |    2   |        ❌¹       |    ✅   | Непроверенный                                                                                                                         |
| HMS-700-2WB   |    2   |        ❌¹       |    ✅   | Непроверенный                                                                                                                         |
| HMS-800-2WB   |    2   |        ❌¹       |    ✅   | **Протестировано** (облако: пиковая нагрузка в реальном времени, профиль сети, включение/выключение + перезагрузка, перезагрузка DTU) |
| HMS-900-2WB   |    2   |        ❌¹       |    ✅   | Непроверенный                                                                                                                         |
| HMS-1000-2WB  |    2   |        ❌¹       |    ✅   | Непроверенный                                                                                                                         |
| HMS-1600-4WB  |    4   |        ❌¹       |    ✅   | Непроверенный                                                                                                                         |
| HMS-1800-4WB  |    4   |        ❌¹       |    ✅   | Непроверенный                                                                                                                         |
| HMS-2000-4WB  |    4   |        ❌¹       |    ✅   | Непроверенный                                                                                                                         |

¹ **Серия WB** (продающаяся как **"HiFlow Pro"** ) не имеет локального TCP-порта — её единственный локальный канал — Bluetooth LE, и все данные передаются в облако Hoymiles. Поэтому эти инверторы работают **только с облаком** : активируйте облачное соединение, и адаптер считывает данные через API S-Miles (импульс в реальном времени, энергия, профиль сети) и может отправлять команды включения/выключения инвертора, перезагрузки и перезагрузки DTU. Все модели WB используют одну и ту же платформу; пока протестирована только модель HMS-800-2WB.

**Работа только в облаке:** любой поддерживаемый инвертор в вашей учетной записи S-Miles также работает без локального подключения — адаптер автоматически обнаруживает его и предоставляет информацию о мощности в реальном времени (пакетный канал), суммарные данные об энергопотреблении, профиль сети, а также информацию о включении/выключении инвертора и его перезагрузке.`inverter.active` /`inverter.reboot` ) плюс перезагрузка DTU (`dtu.reboot` ) команды передаются через облако. Для остальных команд (ограничение мощности, блокировка, очистка предупреждений и т. д.) требуется локальное TCP-соединение.

> Данный адаптер **НЕ** совместим со следующими устройствами: HMS-1600/1800/2000-4T без "DW", сериями HM, MI, внешними модулями DTU или трехфазными моделями HMT.

## Конфигурация

Откройте конфигурацию адаптера в административном интерфейсе ioBroker.

### Локальное соединение (TCP)

| Параметр                                         | По умолчанию | Описание                                                                                                                                                            |
| ------------------------------------------------ | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Включить локальный**                           | на           | Включить прямое соединение TCP/Protobuf. Адаптер поддерживает постоянное TCP-соединение с подтверждением активности через Protobuf.                                 |
| **устройства DTU**                               | (пустой)     | Таблица IP-адресов/имен хостов DTU. Добавьте по одной строке для каждого DTU.                                                                                       |
| **Интервал запроса данных**                      | 5с           | Секунды между запросами данных (0-300). Установите 0 для максимально быстрой обработки (\~1 с за цикл).                                                             |
| **Коэффициент опроса конфигурации/сигнализации** | 6            | Конфигурационные данные и сигналы тревоги запрашиваются каждые N циклов передачи данных.                                                                            |
| **Облачная ретрансляция**                        | на           | Передавайте данные в режиме реального времени в облако Hoymiles от имени DTU. Без этого локальное TCP-соединение блокирует загрузку данных в облако со стороны DTU. |

### Подключение к облаку (S-Miles)

| Параметр                                                        | По умолчанию | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Включить облако**                                             | выключенный  | Включить облачный API Hoymiles S-Miles                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Электронная почта S-Miles**                                   | —            | Адрес электронной почты вашей учетной записи S-Miles                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Пароль S-Miles**                                              | —            | Пароль от вашей учетной записи S-Miles (хранится в зашифрованном виде)                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Быстрая передача данных в режиме реального времени (облако)** | на           | Для инверторов **без** локального подключения данные о потребляемой мощности поступают из облака с высокой частотой в секунду (используется тот же канал «импульсной» передачи данных, что и в режиме реального времени в приложении S-Miles). Обновления`grid.power` и`pvN.power` Примерно каждые 1,5–3 секунды (по указанию сервера) вместо всего лишь каждые \~80 секунд. Это влияет только на устройства, подключенные только к облаку; локально подключенные инверторы сохраняют свои локальные данные в режиме реального времени. |

Все инверторы в вашей облачной учетной записи обнаруживаются автоматически. Ручная настройка серийного номера не требуется.

Оба соединения могут быть включены одновременно. Локальные данные имеют приоритет — облачные данные заполняют данные, когда DTU отключен (например, ночью).

#### Типы учетных записей — S-Miles: установщик / конечный пользователь / домашний

Адаптер принимает учетные записи из всех трех официальных приложений Hoymiles:

- **Установщик S-Miles** (`com.hm.hemaiInstall1` )
- **Пользователь S-Miles** (`com.hm.hemaiClient1` )
- **S-Miles Home** (`com.hm.balcony` )

Вход в систему осуществляется в рамках единого потока v3, за которым следует проверка профиля.`region_c → pre-insp → login → probe` ):

- **Предварительная проверка и вход в систему** определяют вариант аутентификации. Hoymiles объединил все учетные записи на платформе Argon2id.`v=3 + salt` ) в 2026 году, так что`v` больше не является сигналом профиля — сегодня учетные записи установщика, конечного пользователя и домашней учетной записи используют один и тот же запрос Argon2id (параметры из приложения S-Miles Home для Android):`t=3, m=32 MiB, p=1, hashLen=32, V13` Наследие`md5hex(password).sha256base64(password)` Этот вызов сохраняется в качестве запасного варианта для любого региона, который все еще принимает решения.`v=2` .
- **зонд** (`/pvm/.../select_by_page` Затем определяется, к какой платформе API для работы с данными разрешен доступ для учетной записи:
  - Проверка пройдена → профиль **установщика** — учетная запись работает на`global.hoymiles.com` и достигает полной`/pvm/...` веб-API, включая`latitude` /`longitude` /`address` /`local_time` /`status` /`warn_data` и строки версий прошивки.
  - Запрос отклонен (сервер сообщает: _«может использоваться только для входа в приложение S-Miles Home»_ ) → **домашний** профиль — ограничен сервером`/pvmc/.../*_c` Эта поверхность не содержит указанных выше полей, но отображает несколько дополнительных (энергия рефлюкса/самопотребления, цена на электроэнергию). Адаптер **не** создает состояния для отсутствующих полей — они появляются только тогда, когда базовый ответ фактически содержит это значение.`latitude` /`longitude` /`address` средства, возвращаемые на домашние счета, возмещаются через дополнительный фонд.`pvm-ext/station-ak/find` Конечная точка, которую использует само приложение S-Miles Home, обеспечивает работу опроса погоды.

> **Примечание:**`dataeu.hoymiles.com:10081` Это европейская точка доступа к облачной сети, куда DTU отправляют данные — это **не** сервер авторизации пользователей. Адаптер автоматически обрабатывает облачную сеть (см. _Облачная сеть_ ).

#### Тестовый вход в облако

В случае сомнений нажмите кнопку **«Проверить вход в облако»** рядом с полем для пароля. Она запускает четыре этапа один раз с использованием ваших текущих учетных данных.`region_c` ,`pre-insp` ,`login` ,`probe` ) и отчеты`v` а также наличие соли по результатам предварительной проверки, был ли создан токен при входе в систему и какой профиль был назначен зондом (`installer` /`home` Результат записывается в лог, поэтому вы можете вставить его в сообщение об ошибке на форуме. Тест не сохраняет токен и не изменяет состояние адаптера.

## Диспетчер устройств

Адаптер интегрируется с ioBroker **Device Manager** , поэтому каждый инвертор и облачная станция отображаются в виде карточки на вкладке _Device Manager_ в административном интерфейсе — с отображением текущего состояния, элементами управления и диалогом настроек, без необходимости создания собственного представления VIS.

**Что показано**

- Каждый **DTU/инвертор** (`<dtuSerial>` ) и каждая **облачная станция** (`station-<id>` ) адаптер создал.
- **Индикатор активного соединения** (зеленый/красный), а для локально подключенных DTU — уровень сигнала Wi-Fi. В заголовке карты используется название станции (предприятия), к которой принадлежит инвертор — имя, которое вы указали в приложении S-Miles — плюс серийный номер DTU (например,`Zuhause · 4143A01CEDE4` ), что позволяет каждому инвертору быть уникальным; без станции используется название модели или серийный номер.
- **На карте отображаются текущие значения:** мощность (Вт), количество выработанной энергии за сегодня (кВт·ч), мощность каждой фотоэлектрической цепочки, фактически подключенной к инвертору (одна линия на цепочку), и температура инвертора — все данные обновляются автоматически. Станции показывают суммарную мощность и выработанную энергию.
- **Значок** устройства по типу — плоский микроинвертор, вертикальный трехфазный инвертор (линия HMT) или станция. Это оригинальные, нейтральные значки, созданные специально для адаптера, а не изображения продукции производителя. Те же значки используются для объектов устройств в дереве объектов.
- **Индикатор обновления прошивки,** когда облако сообщает об этом (из`dtu.fwUpdateAvailable` ).
- Кнопка **«Подробнее»** открывает панель с подробной информацией только для чтения (серийные номера, версии оборудования/программного обеспечения, модель, уровень сигнала; для станций — вместимость, состояние и адрес).

**Элементы управления** (на каждой плате инвертора)

Карта отображает состояние возможности записи, поэтому щелчок мышью направляет запрос по обычному пути выполнения команды (предпочтительно локальное TCP-соединение, резервный вариант — облако):

- **Переключатели:** включение/выключение инвертора, блокировка инвертора.
- **Ползунки/числа:** ограничение мощности (время работы), ограничение коэффициента мощности, ограничение реактивной мощности, ограничение постоянной мощности, интервал отправки данных в облако.
- **Кнопки (с подтверждением):** перезагрузка инвертора, перезагрузка DTU, сброс предупреждений, сброс ошибки заземления.
- **Настройки** (кнопка, локальные устройства): диалоговое окно для одновременного изменения интервала отправки данных в облако и постоянного ограничения энергопотребления.

Для инверторов, **работающих только через облако** (без локального подключения, например, HMS-800-2WB), отображаются только действия, которые может выполнить облако — включение/выключение инвертора, перезагрузка инвертора, перезагрузка DTU — поскольку остальные команды выполняются только локально. Станции отображают только состояние и подробную информацию (без элементов управления).

**Действия с экземплярами** (над списком устройств): **«Сканировать сеть»** — выполняет поиск DTU в локальной сети и сообщает о найденных устройствах, а **«Проверить вход в облако»** — запускает диагностику входа в систему. (Для обновления списка используется встроенная кнопка обновления Диспетчера устройств.)

> **Примечание:** Устройства, обнаруженные позже в облаке (примерно через 60 секунд после начала), отображаются после нажатия кнопки **«Обновить»** или повторного открытия вкладки; текущий статус уже перечисленных устройств обновляется автоматически.

## Режимы подключения

В зависимости от конфигурации адаптер поддерживает несколько режимов подключения:

|                                    | Только для местных жителей | Локальный + Ретрансляция              | Только облачное хранилище | Локальный + Облачный         | Локальный + Ретрансляция + Облако      |
| ---------------------------------- | -------------------------- | ------------------------------------- | ------------------------- | ---------------------------- | -------------------------------------- |
| **Опрос TCP**                      | да                         | да                                    | —                         | да                           | да                                     |
| **Восстановить соединение**        | Отступление 1–60           | Отступление 1–60                      | —                         | Отступление 1–60             | Отступление 1–60                       |
| **Облачная ретрансляция**          | —                          | HB 60s, данные каждые`serverSendTime` | —                         | —                            | HB 60s, данные каждые`serverSendTime`  |
| **Облако при запуске**             | —                          | —                                     | Полная загрузка           | Полная загрузка              | Полная загрузка                        |
| **Облако (WR онлайн)**             | —                          | —                                     | Каждые 5 минут            | Каждый`serverSendTime`       | 30 секунд после отправки ретранслятора |
| **Облачное хранилище (WR офлайн)** | —                          | —                                     | Каждые 5 минут            | Только погода и пресная вода | Только погода и пресная вода           |

### Автоматическое переподключение

Инвертор (DTU) доступен только тогда, когда вырабатывает электроэнергию (светит солнце). Адаптер автоматически переподключается с экспоненциальным уменьшением времени задержки (1 с, 2 с, 4 с, ... до 60 с максимум). При успешном подключении время задержки сбрасывается до 1 с.

### Ночной режим

Когда локальное соединение обрывается (обычно на закате), адаптер переходит в **ночной режим** :

- Облачный ретранслятор приостанавливает работу (отправляет последнюю загрузку данных, затем отключается).
- Облачный API ограничивается только обновлениями погоды и проверками прошивки (данные в реальном времени отсутствуют, поскольку ничего не меняется).
- После восстановления локального соединения (восхода солнца) адаптер выходит из ночного режима и возобновляет нормальную работу.

### Государственное качество

Адаптер использует атрибут качества состояния ioBroker (`q` ) для указания достоверности и источника данных:

| Качество                 | Ценить      | Значение                                         | Когда                                                                                                                                                                                                                                                                      |
| ------------------------ | ----------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Хороший                  | `0x00` (0)  | Свежие данные, полученные из местных источников. | Нормальный режим работы — данные принимаются напрямую от DTU по протоколу TCP.                                                                                                                                                                                             |
| Заменять                 | `0x40` (64) | резервные данные из облачных источников          | Данные инвертора получаются из облачного API Hoymiles, а не по локальному TCP-соединению (для устройств, работающих только в облаке).                                                                                                                                      |
| Устройство не подключено | `0x42` (66) | Устаревшие данные, устройство отключено.         | Соединение DTU потеряно — значения представляют собой последние известные показания перед разрывом соединения. Также установлено на облачной станции.`grid.*` когда последняя загрузка данных в облако станции произошла более \~20 минут назад (DTU не загружает данные). |

**Затронутые штаты:**`grid.*` ,`pv*.*` ,`inverter.temperature` ,`inverter.active` ,`inverter.warnCount` ,`inverter.warnMessage` ,`inverter.activePowerLimit` ,`meter.*` — плюс измерения облачной станции`station-<id>.grid.*` (отмечено)`0x42` (пока станция не работает/не работает).

Информация гласит (`info.*` ), состояния конфигурации (`config.*` ), а статические облачные данные на уровне станции (название, адрес, координаты, предупреждающие флаги) **не** затрагиваются изменениями качества.

**Автоматический сброс:** При восстановлении локального соединения DTU следующий успешный ответ на запрос данных автоматически вернет все затронутые состояния к исходному уровню качества.`0x00` (хорошо). Аналогично, когда облачная станция возобновляет загрузку, её`grid.*` качество возвращается`0x00` и адаптер немедленно выполняет полное обновление (подробности, устройства, микропрограмма, предупреждения), после чего возобновляет обычный режим опроса.

Атрибут качества можно использовать в скриптах и визуализациях для различения свежих и устаревших данных, например, путем затемнения или затемнения значений.`q > 0` .

## Несколько инверторов

Этот адаптер поддерживает одновременное подключение нескольких инверторов:

- **Локальный режим:** Добавьте несколько IP-адресов DTU в таблицу устройств.
- **Облачное хранилище:** Все инверторы и станции в вашей учетной записи обнаруживаются автоматически.

Каждый DTU создает узел устройства, используя свой серийный номер в качестве идентификатора:

```
hoymiles.0.4143A01CEDE4.grid.power
hoymiles.0.4143A01CEDE4.inverter.*
hoymiles.0.4143A01CEDE4.dtu.*
hoymiles.0.4143A01CEDE4.pv0.*
```

Облачные станции создают агрегированные узлы устройств:

```
hoymiles.0.station-12345.grid.power      ← Sum of all inverters
hoymiles.0.station-12345.grid.totalEnergy
hoymiles.0.station-12345.info.stationName
```

## Штаты

### `<dtuSerial>.grid.*` — Выработка электроэнергии в сети (на единицу DTU)

| Состояние            | Тип   | Единица | Описание                   |
| -------------------- | ----- | ------- | -------------------------- |
| `grid.power`         | число | В       | Выходная мощность сети     |
| `grid.voltage`       | число | В       | Напряжение сети            |
| `grid.current`       | число | А       | Ток сети                   |
| `grid.frequency`     | число | Гц      | Частота сети               |
| `grid.reactivePower` | число | вар     | Реактивная мощность        |
| `grid.powerFactor`   | число | —       | коэффициент мощности       |
| `grid.dailyEnergy`   | число | кВтч    | Суточная выработка энергии |

### `<dtuSerial>.info.*` — Информация об устройстве (по DTU)

| Состояние           | Тип        | Описание                                                       |
| ------------------- | ---------- | -------------------------------------------------------------- |
| `info.connected`    | логический | Подключенное устройство (локальное или облачное)               |
| `info.lastResponse` | число      | Время последнего ответа (метка времени Unix, только локальное) |

### `<dtuSerial>.pv0.*` /`pv1.*` / … — Входы фотоэлектрических панелей (на DTU)

Фотоэлектрические каналы создаются динамически, по одному на каждый вход инвертора.`pv0` …`pv11` (максимум 12).

Количество адаптеров определяется следующим образом:

1. **Локальная информация:** получена из собственных данных инвертора.
2. **Облачные данные:** из словаря правил Hoymiles, поиск осуществляется по префиксу серийного номера инвертора — тому же источнику, который использует приложение S-Miles. Это охватывает все линейки продукции, включая`…-2WB` /`…-4WB` .
3. **Резервный вариант:** по названию модели (`…-2T` ,`…-4WB` , …), или по количеству строк, которые фактически отображаются в данных в реальном времени.

| Состояние         | Тип   | Единица | Описание                                                                  |
| ----------------- | ----- | ------- | ------------------------------------------------------------------------- |
| `pvX.power`       | число | В       | Панель питания                                                            |
| `pvX.voltage`     | число | В       | Напряжение панели                                                         |
| `pvX.current`     | число | А       | Текущая панель                                                            |
| `pvX.dailyEnergy` | число | кВтч    | Ежедневная энергия (только для местного потребления)                      |
| `pvX.totalEnergy` | число | кВтч    | Суммарная энергия (только местная)                                        |
| `pvX.errorCode`   | число |         | Код ошибки для каждой строки, 0 в обычном режиме работы (только локально) |

### `<dtuSerial>.inverter.*` — Статус и управление инвертором (для каждого DTU)

| Состояние                        | Тип        | Единица | Записываемый | Описание                                                                                                                                                                                                                                                            |
| -------------------------------- | ---------- | ------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `inverter.serialNumber`          | нить       | —       | нет          | Серийный номер инвертора                                                                                                                                                                                                                                            |
| `inverter.model`                 | нить       | —       | нет          | Модель инвертора (облачная)                                                                                                                                                                                                                                         |
| `inverter.hwVersion`             | нить       | —       | нет          | Аппаратная версия                                                                                                                                                                                                                                                   |
| `inverter.swVersion`             | нить       | —       | нет          | Версия программного обеспечения                                                                                                                                                                                                                                     |
| `inverter.temperature`           | число      | °С      | нет          | Температура инвертора                                                                                                                                                                                                                                               |
| `inverter.powerLimit`            | число      | %       | **да**       | Ограничение энергопотребления **во время работы** (только ОЗУ, **без износа флэш-памяти/NVM — безопасная запись каждую секунду** ). **Используйте это состояние для реализации нулевого экспорта (Nulleinspeisung)** / динамического ограничения. 2-100%, локальное |
| `inverter.activePowerLimit`      | число      | %       | нет          | Ограничение активной мощности (в режиме реального времени, локально)                                                                                                                                                                                                |
| `inverter.active`                | логический | —       | **да**       | Включение/выключение инвертора (локально; через облако для устройств, работающих только с облаком)                                                                                                                                                                  |
| `inverter.reboot`                | логический | —       | **да**       | Перезагрузите инвертор (кнопка, локально; через облако для устройств, работающих только с облаком).                                                                                                                                                                 |
| `inverter.powerFactorLimit`      | число      | —       | **да**       | Ограничение коэффициента мощности (-1 до 1, местное)                                                                                                                                                                                                                |
| `inverter.reactivePowerLimit`    | число      | °       | **да**       | Ограничение реактивной мощности (-50 до 50, местное)                                                                                                                                                                                                                |
| `inverter.cleanWarnings`         | логический | —       | **да**       | Чистые предупреждения (кнопка, локальные)                                                                                                                                                                                                                           |
| `inverter.cleanGroundingFault`   | логический | —       | **да**       | Устранение замыкания на землю (кнопка, местный контакт)                                                                                                                                                                                                             |
| `inverter.lock`                  | логический | —       | **да**       | Блокировка/разблокировка инвертора (местного)                                                                                                                                                                                                                       |
| `inverter.warnCount`             | число      | —       | нет          | СГСМО`warning_number` поле, необработанное значение (локальное) — недокументированный код предупреждения                                                                                                                                                            |
| `inverter.warnMessage`           | нить       | —       | нет          | Активное предупреждающее сообщение из списка локальных аварийных сигналов WCode.                                                                                                                                                                                    |
| `inverter.linkStatus`            | число      | —       | нет          | Статус ссылки                                                                                                                                                                                                                                                       |
| `inverter.modulationIndexSignal` | число      | —       | нет          | SGSMO #20, необработанное упакованное значение (индекс модуляции + сигнал; точное декодирование еще не подтверждено, локальное)                                                                                                                                     |

### `<dtuSerial>.dtu.*` — Информация DTU (по данным DTU, только для местного использования, за исключением`dtu.reboot` )

| Состояние               | Тип        | Единица | Описание                                                                                                                                                                                                               |
| ----------------------- | ---------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `dtu.serialNumber`      | нить       | —       | серийный номер DTU                                                                                                                                                                                                     |
| `dtu.swVersion`         | нить       | —       | Версия программного обеспечения                                                                                                                                                                                        |
| `dtu.hwVersion`         | нить       | —       | Аппаратная версия                                                                                                                                                                                                      |
| `dtu.rssi`              | число      | дБм     | Уровень сигнала                                                                                                                                                                                                        |
| `dtu.reboot`            | логический | —       | Перезагрузка DTU ( **доступна для записи** , кнопка). Отправляется по локальному TCP-соединению при подключении, в противном случае — через облако для устройств, работающих только с облаком (например, HMS-800-2WB). |
| `dtu.wifiVersion`       | нить       | —       | версия Wi-Fi                                                                                                                                                                                                           |
| `dtu.fwUpdateAvailable` | логический | —       | Доступно обновление прошивки (проверяется один раз в день через облако).                                                                                                                                               |
| `dtu.stepTime`          | число      | с       | Время шага                                                                                                                                                                                                             |
| `dtu.accessModel`       | число      | —       | Режим доступа к сети (0=GPRS, 1=Wi-Fi, 2=Ethernet)                                                                                                                                                                     |
| `dtu.communicationTime` | число      | —       | Последнее сообщение (метка времени Unix)                                                                                                                                                                               |
| `dtu.connState`         | число      | —       | Код ошибки DTU (0=OK)                                                                                                                                                                                                  |
| `dtu.searchResult`      | нить       | —       | Результаты автопоиска (серийные номера инверторов, JSON)                                                                                                                                                               |

### `station-<id>.grid.*` — Сводные данные по станциям (облако)

| Состояние               | Тип        | Единица | Описание                                                                                                                                                 |
| ----------------------- | ---------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `grid.power`            | число      | В       | Суммарная мощность станции (в режиме реального времени примерно за 1,5–3 с через пакетный канал, когда он активен, в противном случае примерно за 80 с). |
| `grid.gridPower`        | число      | В       | Мощность обмена в сети (в реальном времени, +импорт/−экспорт) — ненулевое значение только в системах с учетом потребления.                               |
| `grid.loadPower`        | число      | В       | Потребляемая мощность/нагрузка (в реальном времени)                                                                                                      |
| `grid.batteryPower`     | число      | В       | Питание от батареи (в реальном времени, +заряд/−разряд) — только для аккумуляторных систем.                                                              |
| `grid.pvUtilization`    | число      | %       | Использование фотоэлектрической энергии (в реальном времени)                                                                                             |
| `grid.dailyEnergy`      | число      | кВтч    | Ежедневная энергия                                                                                                                                       |
| `grid.monthEnergy`      | число      | кВтч    | Ежемесячная энергия                                                                                                                                      |
| `grid.yearEnergy`       | число      | кВтч    | Годовая энергия                                                                                                                                          |
| `grid.totalEnergy`      | число      | кВтч    | Суммарная энергия за весь срок службы                                                                                                                    |
| `grid.co2Saved`         | число      | кг      | Сэкономленные CO2                                                                                                                                        |
| `grid.treesPlanted`     | число      | —       | Посажено эквивалентное количество деревьев                                                                                                               |
| `grid.electricityPrice` | число      | /кВтч   | Цена на электроэнергию                                                                                                                                   |
| `grid.currency`         | нить       | —       | Код валюты                                                                                                                                               |
| `grid.isBalance`        | логический | —       | Нулевой экспорт активен                                                                                                                                  |
| `grid.isReflux`         | логический | —       | Активная подача корма                                                                                                                                    |
| `grid.todayIncome`      | число      | —       | Сегодняшний доход                                                                                                                                        |
| `grid.totalIncome`      | число      | —       | Совокупный доход                                                                                                                                         |

### `station-<id>.info.*` — Информация о станции (облако)

| Состояние              | Тип   | Описание                                      |
| ---------------------- | ----- | --------------------------------------------- |
| `info.stationName`     | нить  | Название станции                              |
| `info.stationId`       | число | Идентификатор станции                         |
| `info.systemCapacity`  | число | Мощность системы (кВтп)                       |
| `info.address`         | нить  | Адрес станции                                 |
| `info.latitude`        | число | широта GPS                                    |
| `info.longitude`       | число | GPS долгота                                   |
| `info.stationStatus`   | число | статус станции                                |
| `info.installedAt`     | число | Дата установки                                |
| `info.timezone`        | нить  | Часовой пояс                                  |
| `info.lastCloudUpdate` | число | Время последнего обновления облачных сервисов |
| `info.lastDataTime`    | число | Последнее время получения данных DTU          |

### `station-<id>.weather.*` — Погода на станции (облачность)

| Состояние             | Тип   | Единица | Описание                                                                              |
| --------------------- | ----- | ------- | ------------------------------------------------------------------------------------- |
| `weather.icon`        | нить  | —       | Код значка погоды ( [OpenWeatherMap](https://openweathermap.org/weather-conditions) ) |
| `weather.description` | нить  | —       | Описание погоды на немецком языке (например, «Klarer Himmel», «Regen»)                |
| `weather.temperature` | число | °С      | Текущая температура в месте расположения станции                                      |
| `weather.sunrise`     | число | —       | Время восхода солнца (метка времени Unix, миллисекунды)                               |
| `weather.sunset`      | число | —       | Время захода солнца (метка времени Unix, миллисекунды)                                |

> **Коды значков погоды:** Коды значков соответствуют [соглашениям OpenWeatherMap](https://openweathermap.org/weather-conditions) . Чтобы отобразить значок в виде изображения, используйте:`https://openweathermap.org/img/wn/{icon}@2x.png`

### `station-<id>.warn.*` — Предупреждения для станций (облачность)

Предупреждающие сигналы на уровне сети и счетчиков из облака.`station/find` запись. Все значения логические —`true` означает, что условие в данный момент активно. Учетные записи установщиков считывают флаги из`station/find` ; на счетах S-Miles Home (где`find_c` (опускает их) адаптер переключается на другой вариант`realtime_c` ответ. Этот резервный блок содержит шесть флагов — но **не**`warn.powerLimited` который существует только в установщике.`station/find` запись — и так далее по домашним счетам`warn.powerLimited` пребывание`false` даже когда ограничение движения активно. Состояния отображаются только после того, как облако предоставит данные.`warn_data` блокировка из любого источника.

| Состояние              | Тип        | Описание                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `warn.stationOffline`  | логический | Станция отключена / напряжение питания отключено. Проверено по актуальности данных в реальном времени: станция, которая все еще загружает свежие данные, никогда не сообщается об отключении, даже если облако кратковременно сигнализирует об этом.`s_uoff` (например, когда облачный ретранслятор берет на себя восходящий канал DTU при запуске адаптера) |
| `warn.gridUnstable`    | логический | Нестабильное напряжение в сети                                                                                                                                                                                                                                                                                                                               |
| `warn.gridFault`       | логический | Сбой в сети / неисправность сети                                                                                                                                                                                                                                                                                                                             |
| `warn.deviceAlarm`     | логический | Сигнализация инвертора — в инверторе обнаружена активная неисправность (например, "PVx нет входа" при отключении цепи постоянного тока). Та же проблема проявляется локально и быстрее при других условиях.`alarms.lastCode` / `alarms.lastMessage`                                                                                                          |
| `warn.deviceIdWarning` | логический | Предупреждение об идентификаторе устройства (несоответствие идентификаторов / защита от кражи)                                                                                                                                                                                                                                                               |
| `warn.meterFault`      | логический | Неисправность счетчика / предупреждение о неисправности счетчика                                                                                                                                                                                                                                                                                             |
| `warn.powerLimited`    | логический | Ограничение выходной мощности (активно ограничение/лимит мощности). **Только для учетных записей установщиков** — не предоставляется для домашних учетных записей.                                                                                                                                                                                           |

### `<dtuSerial>.alarms.*` — Данные аварийных сигналов (по DTU, локальные)

| Состояние              | Тип        | Описание                                                                                       |
| ---------------------- | ---------- | ---------------------------------------------------------------------------------------------- |
| `alarms.count`         | число      | Общее количество сигналов тревоги                                                              |
| `alarms.activeCount`   | число      | Количество активных (неразрешенных) сигналов тревоги                                           |
| `alarms.hasActive`     | логический | Имеет активные сигналы тревоги.                                                                |
| `alarms.json`          | нить       | Полный список оповещений в формате JSON.                                                       |
| `alarms.lastCode`      | число      | Последний код тревоги                                                                          |
| `alarms.lastStartTime` | число      | Последнее время срабатывания будильника                                                        |
| `alarms.lastEndTime`   | число      | Время окончания последнего будильника                                                          |
| `alarms.lastMessage`   | нить       | Последнее тревожное сообщение (на языке системы ioBroker, в противном случае — на английском). |
| `alarms.lastData1`     | число      | Последние данные тревоги 1 (исходное значение датчика)                                         |
| `alarms.lastData2`     | число      | Последние данные тревоги 2 (исходное значение датчика)                                         |

### `<dtuSerial>.config.*` — Конфигурация DTU (для каждого DTU, локально)

> ⚠️ **ВНИМАНИЕ — никогда не пишите`config.*` штаты (особенно)`config.limitPowerMyPower` Часто или в автоматическом цикле.** Каждая запись программирует **внутреннюю флэш-память встроенного модуля DTU** (модуля Wi-Fi внутри HMS-xT). Ресурс флэш-памяти ограничен (≈ десятки тысяч циклов); многократные высокочастотные записи — например, цикл с нулевым экспортом каждую секунду — изнашивают её и могут **навсегда вывести устройство из строя** . Используйте эти состояния только для эпизодических, постоянных настроек. **Для динамического/частого ограничения мощности (нулевой экспорт) используйте`inverter.powerLimit` Вместо этого** — команда, выполняемая во время работы, использующая только оперативную память, **без записи во флэш-память и без износа** , безопасная для обновления каждую секунду.

| Состояние                  | Тип   | Единица | Записываемый | Описание                                                                                                                                                                                                                                                                                                                              |
| -------------------------- | ----- | ------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `config.serverDomain`      | нить  | —       | нет          | Домен облачного сервера                                                                                                                                                                                                                                                                                                               |
| `config.serverPort`        | число | —       | нет          | порт облачного сервера                                                                                                                                                                                                                                                                                                                |
| `config.serverSendTime`    | число | мин     | **да**       | Интервал отправки в облако (минуты). ⚠️ Постоянная (флэш-память DTU) — не записывайте часто, см. предупреждение выше.                                                                                                                                                                                                                 |
| `config.limitPowerMyPower` | число | %       | **да**       | **Постоянное** ограничение энергопотребления (хранится во флэш-памяти DTU, сохраняется после перезагрузки; 2-100%, локально). ⚠️ **Только постоянное ограничение — никогда не записывайте в цикле (изнашивает флэш-память DTU). Для динамического использования с нулевым экспортом.`inverter.powerLimit`** (см. предупреждение выше) |
| `config.wifiSsid`          | нить  | —       | нет          | SSID Wi-Fi                                                                                                                                                                                                                                                                                                                            |
| `config.wifiRssi`          | число | дБм     | нет          | Уровень сигнала Wi-Fi (в реальных дБм, например, −65)                                                                                                                                                                                                                                                                                 |
| `config.invType`           | число | —       | нет          | Тип инвертора                                                                                                                                                                                                                                                                                                                         |
| `config.netmodeSelect`     | число | —       | нет          | Режим сети (0=GPRS, 1=Wi-Fi, 2=Ethernet)                                                                                                                                                                                                                                                                                              |
| `config.netDhcpSwitch`     | число | —       | нет          | DHCP включен                                                                                                                                                                                                                                                                                                                          |
| `config.wifiIpAddress`     | нить  | —       | нет          | IP-адрес Wi-Fi                                                                                                                                                                                                                                                                                                                        |
| `config.wifiMacAddress`    | нить  | —       | нет          | MAC-адрес Wi-Fi                                                                                                                                                                                                                                                                                                                       |
| `config.dtuApSsid`         | нить  | —       | нет          | SSID точки доступа DTU                                                                                                                                                                                                                                                                                                                |

### `<dtuSerial>.gridProfile.*` — Профиль сети (для каждого DTU, локально — считывается через облако для устройств, работающих только с облаком)

Профиль подключения инвертора к сети (параметры безопасности/соответствия сетевым нормам), считываемый локально через DevConfigFetch. Все данные доступны только для чтения. Значения напряжения/частоты соответствуют действующим сетевым стандартам (например,`DE_VDE4105_2018` ). Флаги функций являются логическими значениями (`true` = функция активна).

| Состояние                                | Тип        | Единица | Записываемый | Описание                                                                  |
| ---------------------------------------- | ---------- | ------- | ------------ | ------------------------------------------------------------------------- |
| `gridProfile.standard`                   | нить       | —       | нет          | Название стандарта сетки (например, DE\_VDE4105\_2018)                    |
| `gridProfile.countryStdCode`             | число      | —       | нет          | Стандартный код страны                                                    |
| `gridProfile.version`                    | число      | —       | нет          | Версия профиля сетки                                                      |
| `gridProfile.nominalVoltage`             | число      | В       | нет          | Номинальное напряжение                                                    |
| `gridProfile.lowVoltage1`                | число      | В       | нет          | Низкое напряжение 1 (LV1)                                                 |
| `gridProfile.lowVoltage1TripTime`        | число      | с       | нет          | LV1 максимальное время в пути                                             |
| `gridProfile.highVoltage1`               | число      | В       | нет          | Высокое напряжение 1 (ВН1)                                                |
| `gridProfile.highVoltage1TripTime`       | число      | с       | нет          | Максимальное время срабатывания HV1                                       |
| `gridProfile.lowVoltage2`                | число      | В       | нет          | Низковольтный 2 (LV2)                                                     |
| `gridProfile.lowVoltage2TripTime`        | число      | с       | нет          | Максимальное время срабатывания LV2                                       |
| `gridProfile.avgHighVoltage10min`        | число      | В       | нет          | 10-минутное среднее высокое напряжение                                    |
| `gridProfile.nominalFrequency`           | число      | Гц      | нет          | Номинальная частота                                                       |
| `gridProfile.lowFrequency1`              | число      | Гц      | нет          | Низкая частота 1 (LF1)                                                    |
| `gridProfile.lowFrequency1TripTime`      | число      | с       | нет          | LF1 максимальное время в пути                                             |
| `gridProfile.highFrequency1`             | число      | Гц      | нет          | Высокая частота 1 (ВЧ1)                                                   |
| `gridProfile.highFrequency1TripTime`     | число      | с       | нет          | HF1 максимальное время срабатывания                                       |
| `gridProfile.islandingDetection`         | логический | —       | нет          | Активно обнаружение островного режима                                     |
| `gridProfile.reconnectTime`              | число      | с       | нет          | Время повторного подключения                                              |
| `gridProfile.reconnectHighVoltage`       | число      | В       | нет          | Подключите высоковольтное напряжение                                      |
| `gridProfile.reconnectLowVoltage`        | число      | В       | нет          | Подключите низкое напряжение                                              |
| `gridProfile.reconnectHighFrequency`     | число      | Гц      | нет          | Восстановите высокочастотное соединение                                   |
| `gridProfile.reconnectLowFrequency`      | число      | Гц      | нет          | Восстановите низкочастотное соединение                                    |
| `gridProfile.rampUpRateNormal`           | число      | %/с     | нет          | Нормальная скорость наращивания мощности                                  |
| `gridProfile.rampUpRateSoftStart`        | число      | %/с     | нет          | Скорость плавного пуска и нарастания мощности                             |
| `gridProfile.freqWattActive`             | логический | —       | нет          | Активная частота-ватт                                                     |
| `gridProfile.freqWattStart`              | число      | Гц      | нет          | Частотно-ваттный пуск (Fstart)                                            |
| `gridProfile.freqWattDroopSlope`         | число      | %Pn/Гц  | нет          | Наклон кривой зависимости частоты от мощности (Frequency-Watt drop slope) |
| `gridProfile.recoveryRampRate`           | число      | %Пн/с   | нет          | скорость восстановления                                                   |
| `gridProfile.recoveryHighFrequency`      | число      | Гц      | нет          | Восстановление высокой частоты                                            |
| `gridProfile.recoveryLowFrequency`       | число      | Гц      | нет          | Восстановление низкой частоты                                             |
| `gridProfile.activePowerControlActive`   | логический | —       | нет          | Активное управление мощностью                                             |
| `gridProfile.powerRampRate`              | число      | %Пн/с   | нет          | скорость нарастания мощности                                              |
| `gridProfile.voltVarActive`              | логический | —       | нет          | Вольт-Вар активный                                                        |
| `gridProfile.voltVarV1`                  | число      | В       | нет          | Уставка Вольт-Вар V1                                                      |
| `gridProfile.voltVarQ1`                  | число      | %Pn     | нет          | Уставка напряжения-реактива Q1                                            |
| `gridProfile.voltVarV2`                  | число      | В       | нет          | Уставка Вольт-Вар V2                                                      |
| `gridProfile.voltVarV3`                  | число      | В       | нет          | Уставка Вольт-Вар V3                                                      |
| `gridProfile.voltVarV4`                  | число      | В       | нет          | Уставка Вольт-Вар V4                                                      |
| `gridProfile.voltVarQ4`                  | число      | %Pn     | нет          | Уставка вольт-вар Q4                                                      |
| `gridProfile.specifiedPowerFactorActive` | логический | —       | нет          | Активен заданный коэффициент мощности                                     |
| `gridProfile.powerFactor`                | число      | —       | нет          | Коэффициент мощности (cos φ)                                              |
| `gridProfile.wattPowerFactorActive`      | логический | —       | нет          | Активный коэффициент мощности в ваттах                                    |
| `gridProfile.wattPowerFactorStart`       | число      | %Pn     | нет          | Пусковая мощность Ватт-ПФ                                                 |
| `gridProfile.powerFactorAtRatedPower`    | число      | —       | нет          | Коэффициент мощности при номинальной мощности                             |
| `gridProfile.reactivePowerControlActive` | логический | —       | нет          | Активное управление реактивной мощностью                                  |
| `gridProfile.reactivePower`              | число      | %Sn     | нет          | Реактивная мощность (ВАР)                                                 |

### `<dtuSerial>.meter.*` — Счётчик энергии (на единицу DTU, локальный, динамический)

Состояния счетчика создаются автоматически при первом получении данных от DTU. Доступно только при подключении совместимого счетчика электроэнергии.

| Состояние                 | Тип   | Единица | Описание                                     |
| ------------------------- | ----- | ------- | -------------------------------------------- |
| `meter.totalPower`        | число | В       | Суммарная мощность (по всем фазам)           |
| `meter.phaseAPower`       | число | В       | Фаза А питания                               |
| `meter.phaseBPower`       | число | В       | Питание фазы B                               |
| `meter.phaseCPower`       | число | В       | Фаза C питания                               |
| `meter.powerFactorTotal`  | число | —       | суммарный коэффициент мощности               |
| `meter.energyTotalExport` | число | кВтч    | Общий объем экспорта энергии (подача в сеть) |
| `meter.energyTotalImport` | число | кВтч    | Общий объем импорта (потребления) энергии    |
| `meter.voltagePhaseA`     | число | В       | Фаза напряжения А                            |
| `meter.voltagePhaseB`     | число | В       | Фаза напряжения B                            |
| `meter.voltagePhaseC`     | число | В       | Фаза напряжения C                            |
| `meter.currentPhaseA`     | число | А       | Текущая фаза А                               |
| `meter.currentPhaseB`     | число | А       | Текущая фаза B                               |
| `meter.currentPhaseC`     | число | А       | Текущая фаза C                               |
| `meter.faultCode`         | число | —       | Код ошибки счетчика                          |

### Состояния на уровне адаптера

| Состояние             | Тип        | Описание                                                                                                                                                                                     |
| --------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `info.connection`     | логический | Любое подключенное устройство (локальное или облачное)                                                                                                                                       |
| `info.cloudConnected` | логический | Облачный API подключен                                                                                                                                                                       |
| `info.cloudLastError` | нить       | Последняя постоянная ошибка входа в облако (пустое значение, если вход выполнен успешно). Непустые значения приостанавливают автоматические повторные попытки до исправления учетных данных. |

## Протокол

### Локальный (TCP/Protobuf)

- **Транспорт:** TCP-порт 10081
- **Кодировка:** Протокол Buffers (protobuf)
- **Кадр:** 10-байтовый заголовок (`HM` magic + command ID + CRC16 + length) + protobuf payload, with sequence numbers (0-60000)
- **Аутентификация:** отсутствует (только в локальной сети)
- **Шифрование:** опционально AES-128-CBC с выводом ключа SHA-256 (определяется автоматически через информационный ответ DTU).
- **Пульс:** Protobuf отправляет пульс каждые 20 секунд для поддержания постоянного соединения.
- **Повторное подключение:** 5-минутный таймаут простоя, автоматическое повторное подключение с экспоненциальной задержкой (1–60 секунд).

### Облачные технологии (API S-Miles)

- **Базовый URL:**`https://neapi.hoymiles.com`
- **Аутентификация:** хэш учетных данных MD5+SHA256 с одноразовым кодом (nonce).
- **Данные:** данные о станции в режиме реального времени, дерево устройств, подробная информация о станции.
- **Пароль:** хранится в зашифрованном виде в конфигурации ioBroker.

### Благодарности

Обратное проектирование протокола сообществом:

- [hoymiles-wifi](https://github.com/suaveolent/hoymiles-wifi) — библиотека Python (основной источник информации)
- [dtuGateway](https://github.com/ohAnd/dtuGateway) — шлюз для ESP32
- [Hoymiles-DTU-Proto](https://github.com/henkwiedig/Hoymiles-DTU-Proto) — Оригинальные определения protobuf

## Поиск неисправностей

### Адаптер не может подключиться

- Убедитесь, что IP-адрес DTU указан правильно (проверьте таблицу DHCP вашего маршрутизатора).
- Убедитесь, что к порту 10081 не подключено ни одно другое приложение (одновременно допускается только одно подключение).
- Если у вас запущен dtuGateway ESP32, сначала остановите его.

### После подключения данные отсутствуют.

- Прошивка DTU версии V01.01.00 и более поздних может нарушить локальную связь через protobuf.
- НЕ обновляйте прошивку DTU, если для вас важен локальный доступ.
- Проверьте журнал адаптера на наличие ошибок декодирования protobuf.

### Не удалось войти в облако.

- Проверьте свою электронную почту и пароль S-Miles.
- Убедитесь, что вы можете войти в систему по адресу <https://global.hoymiles.com/website/login>
- При возникновении постоянной ошибки аутентификации (неверные учетные данные, заблокированная учетная запись) адаптер останавливает цикл повторных попыток, чтобы избежать дальнейшей блокировки учетной записи. Сообщение об ошибке записывается в файл.`info.cloudLastError` и уведомление об ошибке ioBroker (область действия)`hoymiles` , категория`cloudAuth` Возникает ошибка. Исправьте учетные данные и сохраните конфигурацию, чтобы очистить состояние и возобновить попытки.

### Сообщение об ошибке

Чтобы превратить проблему "не работает" в проблему, которую можно исправить, адаптер выдает сфокусированный, **анонимизированный** диагностический журнал:

1. В административной панели ioBroker откройте настройки экземпляра адаптера и установите **уровень логирования** на`debug` .
2. Перезапустите экземпляр и дайте ему поработать несколько минут (один или два цикла опроса облака).
3. Экспортируйте журнал и выберите строки, помеченные тегами.`[diag]` .

Он`[diag]` В строках передаются необработанные ответы облачного API (процесс входа в систему, список/подробности станций, дерево устройств, данные в реальном времени, прошивка), а также результаты принятия решений адаптером. Серийные номера DTU/инвертора и адрес электронной почты учетной записи заменяются стабильными хеш-токенами, а координаты GPS/адрес/название станции удаляются — таким образом,`[diag]` Эти строки можно безопасно вставлять в сообщения об ошибках на публичных форумах. (Другие, не относящиеся к...)`[diag]` В отладочных строках всё ещё может содержаться реальный последовательный адрес, поэтому отправьте...`[diag]` (конкретно по строкам.)