---
chapters: {"pages":{"en/adapterref/iobroker.luxtronik2-controller/README.md":{"title":{"en":"ioBroker.luxtronik2-controller"},"content":"en/adapterref/iobroker.luxtronik2-controller/README.md"},"en/adapterref/iobroker.luxtronik2-controller/documentation/readme_de.md":{"title":{"en":"no title"},"content":"en/adapterref/iobroker.luxtronik2-controller/documentation/readme_de.md"},"en/adapterref/iobroker.luxtronik2-controller/documentation/readme_en.md":{"title":{"en":"no title"},"content":"en/adapterref/iobroker.luxtronik2-controller/documentation/readme_en.md"}}}
translatedFrom: en
translatedWarning: Если вы хотите отредактировать этот документ, удалите поле «translationFrom», в противном случае этот документ будет снова автоматически переведен
editLink: https://github.com/ioBroker/ioBroker.docs/edit/master/docs/ru/adapterref/iobroker.luxtronik2-controller/documentation/readme_en.md
title: без названия
hash: bdeubyo8ZiCPq/IqLfNhVGk4ilLnm+zlrW1EXUHHEGw=
---
## Настройка экземпляра адаптера

После установки адаптера открывается интерфейс настройки. Он разделен на несколько вкладок для максимальной наглядности процесса настройки.

### 1. Вкладка: Подключение

На этой странице представлены основные сетевые настройки для связи с контроллером Luxtronik, а также язык отображения адаптера.

#### Настройки подключения

- **IP-адрес (хост):** Введите здесь локальный IP-адрес вашего теплового насоса в вашей сети (например, `192.168.178.12`).
- **Порт теплового насоса:** Коммуникационный порт контроллера.
  - `8889` = Стандартный порт для классической TCP-связи (часто используется с прошивкой версии 2.x).
  - `8214` = Порт WebSocket (обычно требуется для более новых систем, начиная с версии прошивки V3.81x).
- **Интервал опроса (секунды):** Задает временной интервал, через который адаптер получает новые измеренные значения и параметры от теплового насоса (по умолчанию: 45 секунд).

> 💡 **ВАЖНЫЙ СОВЕТ ОТНОСИТЕЛЬНО ИНТЕРВАЛА ОПРОСА:** **Не** выбирайте слишком низкое значение! Слишком частый опрос (например, каждые 10 секунд) постоянно перегружает внутренний процессор контроллера Luxtronik запросами. Это приводит к высокой нагрузке на ЦП теплового насоса, что делает работу контроллера (как на сенсорном дисплее, так и в сети) крайне медленной. Рекомендуемые значения находятся в диапазоне от 45 до 60 секунд.

---

#### Параметры

- **Язык для текста и значений:** Этот параметр определяет язык, на котором текстовые состояния и режимы работы записываются в точки данных ioBroker. Адаптер автоматически преобразует числовые коды системы в читаемый текст.

_Пример: Когда тепловой насос нагревает воду, адаптер выдает либо `Warmwasser` (немецкий) или `Hot water` (Английский) в дерево объектов в зависимости от вашего выбора._

![Пример преобразованных значений в дереве объектов ioBroker.](../../../../en/adapterref/iobroker.luxtronik2-controller/admin/img/Objekte.png)

### 2. Вкладка: Оптимизация цикла

По умолчанию контроллер Luxtronik обрабатывает циклы отопления и горячего водоснабжения строго раздельно. Это часто приводит к остановке компрессора после приготовления горячей воды и его повторному запуску вскоре после этого для цикла отопления (повышенный износ). Данный адаптер интеллектуально объединяет эти операции, обеспечивая бесперебойную и эффективную работу компрессора в едином непрерывном цикле.

- **Включение интеллектуальной оптимизации цикла:** активирует общую логику, предотвращающую ненужные остановки компрессора.
  - **Правило срабатывания (предварительное зажигание):** Если горячая вода в бытовом водонагревателе остывает `(DHW Target - DHW Actual ≥ DHW Hysteresis - 1.5 K)` **И** одновременно существует потребность в отоплении. `(Return Actual ≤ Return Target)` В то время как ограничение на летнее отопление не активировано, адаптер вмешивается.
  - **Действие:** Адаптер напрямую запускает режим нагрева и временно устанавливает целевое значение температуры возврата на 35 °C, чтобы принудительно запустить компрессор немедленно. Если система вскоре после этого переключится на горячую воду, компрессор просто продолжит работу.
- **Принудительный нагрев после подачи горячей воды:** При активации адаптер также проверяет систему _после_ цикла подачи горячей воды. Целевое значение температуры возврата остается повышенным до 35°C, чтобы компрессор не отключался после приготовления горячей воды, а немедленно продолжал цикл нагрева.

> **⚠️ Важное примечание:** Если вы используете эту оптимизацию цикла, **настоятельно рекомендуется** включить опцию _«Принудительное использование значений по умолчанию в режиме ожидания»_ на вкладке _«Режим ожидания»_ . Это единственный способ гарантировать, что временно измененное целевое значение 35°C будет корректно возвращено к вашим обычным значениям температуры нагрева в конце цикла!

![Пример оптимизации цикла](../../../../en/adapterref/iobroker.luxtronik2-controller/admin/img/Takt_Optimierung_de.svg)

### 3. Вкладка: Режим ожидания (защита оборудования)

Контроллер Luxtronik сохраняет измененные параметры во внутренней флэш-памяти, которая выдерживает лишь ограниченное количество циклов записи (износ флэш-памяти EEPROM). Для защиты этой памяти адаптер выполняет операции записи только тогда, когда система активно работает (отопление или горячая вода).

Как только тепловой насос переходит в режим **ожидания (холостого хода)** , оптимизация завершается. Чтобы предотвратить дальнейшую работу системы с временными (измененными) параметрами, полученными в результате оптимизации, адаптер принудительно возвращает контроллер в безопасное исходное состояние.

> **💡 Настоятельная рекомендация:** Если вы используете **интеллектуальную оптимизацию цикла** (совмещение горячего водоснабжения и отопления) и/или **динамическое управление HUP** , обязательно включите установку значений по умолчанию в режиме ожидания! Это единственный способ гарантировать, что после вмешательства адаптера система продолжит работать с точно такими же исходными значениями.

- **Значения по умолчанию:** Обязательно введите здесь точные исходные значения по умолчанию вашей системы отопления (например, стандартный гистерезис для отопления/горячего водоснабжения, базовая точка, конечная точка и напряжение насоса).
- **Визуализация кривой нагрева:** Для лучшей ориентации адаптер генерирует графический предварительный просмотр вашей кривой нагрева (целевой точки возврата) в режиме реального времени, как только вы вводите базовую и конечную точки. _(Большое спасибо [mnemotron.de](https://www.mnemotron.de/lux/heatcurve.html) за вдохновение для этого представления!)_

### 4. Вкладка: Циркуляционный насос системы отопления (ЦНН)

Циркуляционный насос системы отопления (ЦНН) перекачивает теплую воду из теплового насоса в отопительный контур. Однако фиксированная производительность насоса неэффективна: если она слишком высока, вода слишком быстро течет по трубам и не может оптимально отдавать тепло в помещение. Если же она слишком низка, вода слишком сильно охлаждается, и тепловой насос теряет эффективность.

Этот адаптер решает данную проблему за счет **динамического управления, основанного на разбросе температур** (разнице между температурой подачи и температурой обратной связи). Во время цикла нагрева напряжение управления насосом увеличивается или уменьшается с небольшими шагами через равные промежутки времени, чтобы всегда оставаться точно в заданном целевом диапазоне.

> **⚠️ Важные предварительные условия (пожалуйста, проверьте их перед активацией!)**
>
> 1. **Совместимость с оборудованием:** Используйте эту функцию только в том случае, если ваш циркуляционный насос подключен к плате Luxtronik через управляющий кабель (0-10 В или ШИМ)! Если у вас насос, который регулирует объемный расход независимо (например, _Grundfos ALPHA2 AutoAdapt,_ установленный в режим «Авто»), **не** активируйте эту функцию. В противном случае адаптер и насос будут постоянно регулироваться друг относительно друга.
> 2. **Коэффициент напряжения (прошивка):** Более старые версии прошивки V2.x ожидают управляющее напряжение в другом формате данных, чем более новые версии прошивки V3.x (например, на LWCV 82). Убедитесь, что в конфигурации выбран правильный аппаратный коэффициент для вашей системы. `100` для версии 2.x по сравнению с... `10` (для версии 3.x).
> 3. **Сброс к защитным настройкам (режим ожидания):** Обязательно включите функцию _«Принудительное использование значений по умолчанию в режиме ожидания»_ на вкладке «Режим ожидания». Это гарантирует, что по окончании цикла нагрева насос вернется к своему фиксированному стандартному напряжению, а не останется на заданном значении.

#### Настройка вашей системы

Оптимальный температурный режим крайне индивидуален для каждого дома и зависит от системы отопления:

- **Система подогрева пола (UFH):** работает с большим объемом воды и низкими температурами. В этом случае оптимальным часто является диапазон температур от **3 до 5 Кельвинов** .
- **Радиаторы:** Требуют более высоких температур подаваемого тепла и значительно охлаждают помещение. В данном случае обычно ожидается разброс температур от **7 до 10 Кельвинов** .

Введите предельные значения, подходящие для вашей системы, в поле _«Минимальное/Максимальное отклонение_ ». Затем адаптер будет проверять каждые _X минут_ (интервал настройки), находится ли отклонение в пределах целевого диапазона. Если отклонение слишком низкое (вода течет слишком быстро), напряжение насоса уменьшается на заданный _шаг_ (например, 0,25 В). Если отклонение слишком высокое, оно плавно увеличивается.

![Пример оптимизации HUP](../../../../en/adapterref/iobroker.luxtronik2-controller/admin/img/HUP_Optimierung_de.svg)

### 5. Вкладка: Циркуляционный насос (ZIP)

Циркуляционный насос (ЦНН) обеспечивает мгновенную подачу горячей воды в краны в доме (например, в душ, раковину). Однако, если он работает непрерывно или слишком часто с помощью таймера, он значительно охлаждает бак для горячей воды (потери энергии) и излишне потребляет электроэнергию.

Этот адаптер предлагает интеллектуальные функции автоматизации, позволяющие запускать ZIP-архив только тогда, когда это действительно необходимо.

- **Интеллектуальная оптимизация ZIP:** при активации адаптер контролирует работу теплового насоса. Таким образом, циркуляция может работать, например, полностью синхронно с подготовкой горячей воды.
- **Время работы после включения:** определяет, как долго (в секундах) насос должен работать при срабатывании адаптера или вручную (через адаптер). `Activate_Zip` (переключатель в дереве объектов). Обычно рекомендуется промывать систему трубопроводов теплой водой один раз с короткими интервалами от 120 до 180 секунд.
- **Датчики движения (по запросу):** абсолютный потенциал экономии! Здесь вы можете ввести данные с датчиков движения вашего умного дома в ioBroker (например, датчики Zigbee в ванной или на кухне). Когда кто-то входит в комнату, адаптер немедленно запускает короткий цикл циркуляции. Вода становится теплой, как только вы встаете у раковины, и энергия не расходуется впустую.
- **Внешние исполнительные механизмы (например, умные розетки):** Если ваш циркуляционный насос подключен не напрямую к плате Luxtronik, а к интеллектуальному реле (например, Shelly, Osram Smart Plug и т. д.), вы можете сохранить данные о розетках здесь. Затем адаптер автоматически включает и выключает ваши WLAN/Zigbee-розетки, используя внутреннюю логику. _(Преимущество: это исключает операции записи во флэш-память теплового насоса!)_

**💡 Совет! Аппаратная защита (износ флэш-памяти EEPROM — обратите внимание!).** Чтобы свести к минимуму постоянную запись в контроллер, установите обычное время ZIP-адреса один раз в таблицу Пн-Вс и введите там 00:00 - 00:00. Установите время цикла: Выкл.: 60 минут и Вкл.: 0 минут.

**Для уменьшения количества операций записи в контроллер рекомендуется управлять ZIP-блоками через внешний исполнительный механизм ➔ 0 операций записи в контроллере!** **Для сравнения:** активация через контроллер Luxtronik2 требует 4 операций записи для программы деаэрации. Использование таблицы управления ZIP требует от 4 (в лучшем случае) до 14 (в худшем случае) операций записи во флэш-память за один цикл ZIP.