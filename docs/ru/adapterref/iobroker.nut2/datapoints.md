---
chapters: {"pages":{"de/adapterref/iobroker.nut2/README.md":{"title":{"de":"ioBroker.nut2 — Einrichtung"},"content":"de/adapterref/iobroker.nut2/README.md"},"de/adapterref/iobroker.nut2/datapoints.md":{"title":{"de":"Datenpunkte"},"content":"de/adapterref/iobroker.nut2/datapoints.md"},"de/adapterref/iobroker.nut2/faq.md":{"title":{"de":"Häufige Fragen"},"content":"de/adapterref/iobroker.nut2/faq.md"}}}
translatedFrom: de
translatedWarning: Если вы хотите отредактировать этот документ, удалите поле «translationFrom», в противном случае этот документ будет снова автоматически переведен
editLink: https://github.com/ioBroker/ioBroker.docs/edit/master/docs/ru/adapterref/iobroker.nut2/datapoints.md
title: Точки данных
hash: P1hiGHF5ZVhP8hlMNn047ImYYDwzOVLI2EtiymCII5U=
---
# Точки данных

Адаптер не поставляется с фиксированным списком параметров. Он запрашивает у сервера NUT информацию о том, что сообщает ваш ИБП (источник бесперебойного питания).`LIST VAR`), и строит дерево на основе ответа — поэтому две разные модели UPS приводят к двум разным деревьям. Далее объясняются части, которые всегда остаются неизменными, и как переменная NUT становится точкой данных ioBroker.

Каждая точка данных сопровождается кратким пояснением. `common.desc`Списки значений, тексты состояния и уровни серьезности отображаются на языке вашей системы ioBroker.

## Преобразование названия нуклеотидной единицы в идентификатор объекта.

В NUT переменные обозначаются точками: `battery.charge.low`ioBroker использует точки для своей иерархии, поэтому адаптер это отражает:

- тот **первый** Точка разделяет пролив — `battery`
- Каждая последующая точка заменяется дефисом — `charge-low`

`battery.charge.low` Это помещает его под `ups0.battery.charge-low`и команда `test.battery.start` под
`ups0.commands.test-battery-start`Переменная без точки (некоторые водители отображают пустую точку). `ALARM`) не имеет канала и располагается непосредственно под устройством.

Адаптер сохраняет фактическое имя NUT внутри себя, так что операция записи отправляет правильное имя обратно на сервер — даже если сопоставление необратимо (например, трехфазные имена...). `input.L1-L2.voltage` (они сами содержат дефис).

## точки данных ширины адаптера

| Точка данных           | Значение                                                                                                            |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `info.connection`      | Адаптер взаимодействует с сервером NUT. Это ничего не говорит об учетных данных для входа.                          |
| `info.upsTotal`        | Сколько источников бесперебойного питания (ИБП) предлагает сервер?                                                  |
| `info.upsReachable`    | Сколько из них ответили на последний вопрос?                                                                        |
| `info.allUpsReachable` | Одно значение, а не каждое устройство. **Неверно**Пока ничего не обнаружено — «0 из 0» не означает «все в порядке». |
| `notify`               | Записываемый триггер. Каждая операция записи немедленно активируется; см. часто задаваемые вопросы.                 |

## Каждый ИБП

Каждый ИБП, подключенный к серверу, становится устройством. Его название происходит от... `desc`, который вы в `ups.conf` сервером; без такой настройки адаптер получает информацию о производителе и модели непосредственно от ИБП.

### `info`

| Точка данных | Значение                                                                                                                                                                                                                                          |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `reachable`  | Этот ИБП ответил на последний запрос. Значение также отображает символ "в сети" в объекте устройства и изменяется на false, когда адаптер останавливается — остановленный адаптер не должен продолжать работу, указывая на то, что все в порядке. |
| `notify`     | Последнее событие upsmon, связанное с этим ИБП.                                                                                                                                                                                                   |

### `status` — оцененное состояние

NUT сообщает о состоянии ИБП в виде последовательности аббревиатур, например: `OL CHRG`Это затрудняет работу с ним в скрипте, поэтому адаптер упрощает задачу:

| Точка данных           | Значение                                                                                                                                                                                                                                                |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `raw`                  | Исходная строка без изменений.                                                                                                                                                                                                                          |
| `display`              | Та же информация, что и в читаемом тексте на вашем языке, например, «В сети, загрузка».                                                                                                                                                                 |
| `severity`             | Одно число, от 0 до 4 (см. ниже).                                                                                                                                                                                                                       |
| 19 значений истинности | По одному для каждого известного флага состояния: `online`, `onBattery`, `lowBattery`, `charging`, `discharging`, `replaceBattery`, `overloaded`, `bypass`, `calibrating`, `forcedShutdown`, `alarm`, `ecoMode`, `testing`, `overheat` и многое другое. |

`charging` и `discharging` также будет изготовлено из `battery.charger.status` заряжен, потому что некоторые модели ИБП отображают уровень заряда там, а не с помощью флажков. `CHRG`/`DISCHRG`.

Требуется аббревиатура, которую адаптер не распознает. **нет** потеряно — оно остается в `raw` и `display` видимо. Спецификация NUT явно разрешает драйверам использовать собственные сокращения, и было бы целесообразно придумать для каждого из них значение истинности.

#### Уровни тяжести

| Уровень | Значение              | Запущено по                                   |
| ------- | --------------------- | --------------------------------------------- |
| 0       | ХОРОШО                | В сети                                        |
| 1       | информация            | Напряжение понижается/повышается, калибровка. |
| 2       | предупреждение        | Батарея, замена батареи, обход                |
| 3       | Критический           | Питание от батареи **и** Низкий заряд батареи |
| 4       | Чрезвычайная ситуация | Принудительное отключение                     |

Уровень тяжести описывает исключительно **Ситуация с электропитанием**Флаги ошибок, такие как `overloaded`, `alarm` и
`off` Они намеренно не поднимают этот вопрос — у них свои собственные значения истинности, и их включение привело бы к тому, что одно число означало бы две разные вещи.

### Измерительные каналы

Наличие тех или иных вариантов полностью зависит от вашего водителя ИБП:

| канал     | Содержит                                                                                                                                                |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `battery` | Заряд, оставшееся время работы, напряжение, тип, температура                                                                                            |
| `device`  | Производитель, модель, серийный номер, тип устройства                                                                                                   |
| `driver`  | Какой драйвер NUT используется для этого ИБП, его версия и параметры.                                                                                   |
| `input`   | Что поступает из сети — напряжение, частота, пороги переключения.                                                                                       |
| `output`  | Что выдает ИБП — напряжение, частота, ток.                                                                                                              |
| `ups`     | Сам ИБП — нагрузка, мощность, температура, задержки, обратный отсчет.                                                                                   |
| `outlet`  | Переключаемые выходы и группы выходов, если модель их имеет.                                                                                            |
| `ambient` | Датчики окружающей среды, если они предусмотрены источником бесперебойного питания (ИБП) или подключенным устройством электромагнитного импульса (ЭМИ). |

Значения хранятся в том виде, в котором они есть на самом деле: числа с единицами измерения (В, Гц, А, Ач, %, Вт, ВА, с, °C), ответы «да/нет» в виде значений истинности, фиксированные словари (`enabled`/`disabled`/`muted`, `charging`/`discharging`/…) в виде списков выбора. Значение, которое должно быть числовым, но таковым не является, отбрасывается с предупреждением вместо того, чтобы быть сохраненным как мусор — с `Infinity` Никакая диаграмма не сможет это объяснить.

Обратный отсчет (`ups.timer.shutdown` и братья и сестры) являются **пустой**Пока не запущен обратный отсчет. Водители сообщают об этом по-разному — некоторые сообщают... `-1`остальные — это слово `NotActive` —, и ни фраза «минус одна секунда», ни опечатка не подойдут.

### `commands`

Одна кнопка на каждую команду, предлагаемая UPS — создается только в том случае, если **активировать команды** на есть _и_ Данные доступа сохраняются. Нажатие клавиши отправляет `INSTCMD` и перезагружается.

Команды, отключающие питание (`load.off`, `shutdown.*`, `bypass.*`), размещают предупреждающий знак в начале своего заявления.

## Кому принадлежат эти предметы?

Имя, описание, тип, роль и единица измерения всех точек данных принадлежат адаптеру — любое переименование в дереве объектов будет сброшено во время следующей синхронизации. **Настройки записи** Исключение составляют следующие данные: они принадлежат вам, к ним никто не прикасается, и если адаптер переименовывает свою точку данных, запись перемещается вместе с ней.

Источник бесперебойного питания (ИБП), исчезающий с сервера NUT, теряет свои объекты; при его возвращении они создаются заново.