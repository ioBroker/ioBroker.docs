import { makeStyles } from '../../theme';

export const useStyles = makeStyles()(theme => ({
    menuBlock: {},
    mainBlock: {},
    menuButton: {},
    mainTopBlock: {},
    adaptersSearch: {},
    adaptersButton: {},
}));
