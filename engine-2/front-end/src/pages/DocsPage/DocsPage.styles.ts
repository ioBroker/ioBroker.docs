import { makeStyles } from '../../theme';

export const useStyles = makeStyles()(theme => ({
    root: {
        display: 'flex',
    },
    menuBlock: {},
    mainBlock: {},
    menuButton: {},
    mainTopBlock: {},
    adaptersSearch: {},
    adaptersButton: {},
}));
